# Messaging Example App

This Flutter project shows an example setup for a messaging application.

## Initial Setup
Create new app
`flutter create -e --project_name messaging_example messaging_flutter`

Initialize git for project
`git init && git add . && git commit -m 'Initial Commit'`

Pin project Flutter version
`fvm use stable --pin`
